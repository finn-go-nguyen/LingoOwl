import 'package:flutter/material.dart';

import '../../../common/colored_sized_box.dart';

class PriceLabelLoading extends StatelessWidget {
  const PriceLabelLoading({super.key});

  @override
  Widget build(BuildContext context) {
    return const ColoredSizedBox(
      height: 26.0,
      width: 150.0,
    );
  }
}
